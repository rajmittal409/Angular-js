import { EmppositionPipe } from './empposition.pipe';

describe('EmppositionPipe', () => {
  it('create an instance', () => {
    const pipe = new EmppositionPipe();
    expect(pipe).toBeTruthy();
  });
});
